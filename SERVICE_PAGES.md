# Служебные страницы A-LAB.TECH

## 📄 Созданные страницы

### 1. **404.html** — Страница "Не найдено"
**Когда использовать**: Автоматически показывается при переходе на несуществующую страницу

**Особенности**:
- ✓ Анимированный код ошибки 404 с эффектом glitch
- ✓ Плавающие частицы на фоне
- ✓ Сетка в стиле матрицы
- ✓ Детали ошибки (timestamp, status code)
- ✓ Кнопка возврата на главную

**Настройка для веб-сервера**:
```nginx
# Nginx
error_page 404 /404.html;
```

```apache
# Apache (.htaccess)
ErrorDocument 404 /404.html
```

---

### 2. **success.html** — Успешная отправка формы
**Когда использовать**: Редирект после успешной отправки любой формы

**Особенности**:
- ✓ Зеленая цветовая схема (success)
- ✓ Анимированная иконка галочки
- ✓ Информация о следующих шагах
- ✓ Уникальный REQUEST_ID для каждой заявки
- ✓ Кнопки возврата на главную и к команде

**Как использовать в формах**:

#### Вариант 1: JavaScript редирект
```javascript
// В обработчике формы после успешной отправки
if (response.ok) {
    window.location.href = 'success.html';
}
```

#### Вариант 2: Через Supabase Edge Function
```javascript
const { error } = await supabase.from('leads').insert([data]);

if (!error) {
    window.location.href = 'success.html';
} else {
    window.location.href = 'error.html';
}
```

---

### 3. **error.html** — Ошибка отправки формы
**Когда использовать**: Редирект при неудачной отправке формы

**Особенности**:
- ✓ Красная цветовая схема (error)
- ✓ Анимированная иконка крестика
- ✓ Список возможных причин ошибки
- ✓ Рекомендации по решению проблемы
- ✓ Кнопка "Попробовать снова" (history.back)
- ✓ Прямая ссылка на Telegram для связи

**Как использовать в формах**:

```javascript
// В обработчике формы при ошибке
if (error) {
    window.location.href = 'error.html';
}
```

---

## 🔧 Интеграция с существующими формами

### Форма "Join Lab" (join.html)

**Текущий код** (примерно строка 450):
```javascript
document.getElementById('joinForm').addEventListener('submit', (e) => {
    e.preventDefault();
    alert('Заявка отправлена!');
});
```

**Обновленный код с редиректами**:
```javascript
document.getElementById('joinForm').addEventListener('submit', async (e) => {
    e.preventDefault();
    
    const formData = {
        name: document.getElementById('name').value,
        contact: document.getElementById('contact').value,
        message: document.getElementById('message').value,
        source: 'join_lab'
    };

    try {
        if (window.ALabCore && window.ALabCore.db) {
            const { error } = await window.ALabCore.db
                .from('leads')
                .insert([formData]);

            if (error) throw error;
            
            // Успех
            window.location.href = 'success.html';
        } else {
            // Fallback если БД не подключена
            console.log('Form data:', formData);
            window.location.href = 'success.html';
        }
    } catch (err) {
        console.error('Form submission error:', err);
        window.location.href = 'error.html';
    }
});
```

---

### Форма на Marketing (marketing.html)

**Найти** (примерно строка 480):
```javascript
document.getElementById('leadForm').addEventListener('submit', (e) => {
    e.preventDefault();
    alert('Запрос отправлен в Telegram!');
});
```

**Заменить на**:
```javascript
document.getElementById('leadForm').addEventListener('submit', async (e) => {
    e.preventDefault();
    
    const formData = {
        name: document.querySelector('input[placeholder*="Имя"]').value,
        contact: document.querySelector('input[placeholder*="Telegram"]').value,
        source: 'marketing_page'
    };

    try {
        if (window.ALabCore && window.ALabCore.db) {
            const { error } = await window.ALabCore.db
                .from('leads')
                .insert([formData]);

            if (error) throw error;
            window.location.href = 'success.html';
        } else {
            window.location.href = 'success.html';
        }
    } catch (err) {
        window.location.href = 'error.html';
    }
});
```

---

### Форма на Design (design.html)

Аналогично marketing.html, заменить `alert()` на редирект с обработкой ошибок.

---

## 🎨 Кастомизация страниц

### Изменение текста

**success.html** — строка 135:
```html
<p class="success-message">
    Ваша заявка успешно отправлена и зашифрована в нашей базе данных.<br>
    Мы получили ваш запрос и <strong>свяжемся с вами в ближайшее время</strong>.
</p>
```

**error.html** — строка 135:
```html
<p class="error-message">
    К сожалению, при отправке вашей заявки произошла ошибка.<br>
    <strong>Данные не были сохранены</strong>. Пожалуйста, попробуйте еще раз.
</p>
```

### Изменение кнопок

Все кнопки имеют класс `.hover-trigger` для работы с кастомным курсором.

**Пример добавления новой кнопки**:
```html
<a href="residents.html" class="btn btn-secondary hover-trigger">Наша команда</a>
```

---

## 📊 Аналитика

### Отслеживание успешных отправок

Добавьте в `success.html` перед `</body>`:
```html
<script>
    // Google Analytics
    if (typeof gtag !== 'undefined') {
        gtag('event', 'form_submission', {
            'event_category': 'engagement',
            'event_label': 'success'
        });
    }
</script>
```

### Отслеживание ошибок

Добавьте в `error.html` перед `</body>`:
```html
<script>
    // Google Analytics
    if (typeof gtag !== 'undefined') {
        gtag('event', 'form_error', {
            'event_category': 'errors',
            'event_label': 'submission_failed'
        });
    }
</script>
```

---

## ✅ Чеклист интеграции

- [ ] Все три страницы созданы (404.html, success.html, error.html)
- [ ] Настроен редирект на 404.html в конфигурации сервера
- [ ] Обновлены обработчики форм в join.html
- [ ] Обновлены обработчики форм в marketing.html
- [ ] Обновлены обработчики форм в design.html
- [ ] Протестирована успешная отправка формы
- [ ] Протестирована ошибка отправки (отключить интернет)
- [ ] Протестирован переход на несуществующую страницу
- [ ] (Опционально) Добавлена аналитика

---

## 🚀 Готово к использованию!

Все служебные страницы созданы в едином стиле A-LAB.TECH с:
- ✓ Премиум дизайном
- ✓ Анимациями
- ✓ Кастомным курсором
- ✓ Адаптивной версткой
- ✓ Информативными сообщениями

**Следующий шаг**: Обновите обработчики форм согласно инструкциям выше.
